<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/constraintLayout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/profile_pic_view"
        android:layout_width="@dimen/reel_tray_imageview_width_and_height"
        android:layout_height="@dimen/reel_tray_imageview_width_and_height"
        android:elevation="1dp"
        android:scaleType="centerCrop"
        android:src="@drawable/lisa"
        app:strokeColor="@color/main"
        app:strokeWidth="3dp"
        android:padding="5dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:shapeAppearance="@style/circularImageView" />

    <TextView
        android:id="@+id/username_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="5dp"
        android:layout_marginTop="5dp"
        android:layout_marginEnd="5dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:gravity="center"
        android:text="mostlysane"
        android:textColor="@color/black"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/profile_pic_view"
        app:layout_constraintTop_toBottomOf="@+id/profile_pic_view" />

</androidx.constraintlayout.widget.ConstraintLayout>